- name: Upload to Pinata
  uses: jamesmoriarty/pinata-action@v1
  with:
    pinata_api_key: ${{ secrets.PINATA_API_KEY }}
    pinata_secret_api_key: ${{ secrets.PINATA_SECRET_API_KEY }}
    pin_directory: './'
